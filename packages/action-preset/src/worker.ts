export const requiredItems = ['worker'];

export function constructor(
  pkg: { [key: string]: unknown },
  variants: { [key: string]: unknown }
): string {
  // TODO - Returns the variants' generator id.
  return '';
}

export function variantsGenerator(id: string) {

}
